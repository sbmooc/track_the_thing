server {
    listen 80;
    server_name 134.209.26.237;

    #location /static/ {
    #    autoindex on;
    #    alias /static/;
    #}

    location / {
        proxy_pass http://app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}